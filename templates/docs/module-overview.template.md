---
docType: module-overview
status: draft
generatedBy: ai
reviewedByHuman: false
needsHumanConfirmation: false
lastReviewedAt: ""
sourceOfTruth: repository
scanSkill: scan-module-by-ai
moduleId: "{{MODULE_ID}}"
moduleDisplayName: "{{MODULE_DISPLAY_NAME}}"
moduleType: "{{MODULE_TYPE}}"
scanScope: "{{SCAN_SCOPE_TEXT}}"
moduleRootRelative: "{{MODULE_ROOT_RELATIVE}}"
primaryPaths: []
relatedPaths: []
entryFiles: []
registryFiles: []
configFiles: []
excludePaths: []
readOnlyPaths: []
testPaths: []
sourceEvidence: []
scannedAt: "{{SCANNED_AT_ISO}}"
---

# 模块概述：{{MODULE_DISPLAY_NAME}}（`{{MODULE_ID}}`）

> **事实源声明**：本文件为辅助上下文。模块行为以 `{projectRoot}/.ai/config/module-map.md` 中本 `moduleId` 声明的路径与关键文件为准；若尚无 map 行，则以本文件 front matter 所列 `primaryPaths`、`entryFiles` 等已读路径为准，其余为推断。

## 扫描范围（scanScope）

{{SCAN_SCOPE_TEXT}}

### 路径与关键文件（与 front matter 一致）

- `primaryPaths`：见 YAML `primaryPaths`。
- `relatedPaths` / `entryFiles` / `registryFiles` / `configFiles`：见对应 YAML 列表。

## 职责边界（推断）

<!-- 基于已读文件归纳 -->

## 对外接口

<!-- 入口类、公开 API、路由注册等；须附路径 -->

## 关键依赖

| 依赖方 | 说明 | 路径 |
|--------|------|------|
| | | |

## 风险点与约定（推断）

## 待确认项

1. 

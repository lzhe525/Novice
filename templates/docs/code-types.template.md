---

docType: module-code-types

status: draft

generatedBy: ai

reviewedByHuman: false

needsHumanConfirmation: false

lastReviewedAt: ""

sourceOfTruth: repository

scanSkill: detect-code-types-by-ai

moduleId: "{{MODULE_ID}}"

scannedAt: "{{SCANNED_AT_ISO}}"

---



# 模块候选代码类型：`{{MODULE_ID}}`



> **事实源声明**：本文件列出的候选代码类型以 `{projectRoot}/.ai/config/module-map.md` 中本 `moduleId` 的声明路径、以及本轮 Skill 在 **scanScope** 内已读的源码样例为准；模块五件套中的 `overview` / `deep-dive` / `constraints` / `change-guide` 为辅助归纳。**非** Pattern Pack，不替代源码。



## 扫描范围摘要（scanScope）



<!-- 与 module-map / 模块文档一致的简短中文说明；可引用 overview front matter 中的路径列表 -->



## 候选代码类型总览



| codeTypeId | 显示名（中文） | evidenceStrength | status | needsHumanConfirmation | 适合抽象为 Pattern Pack（评估） |

|------------|----------------|-------------------|--------|------------------------|--------------------------------|

| | | | | | |



## 候选代码类型明细



### 模板节（每个 codeTypeId 复制一节，锚点建议：`## codeType-{codeTypeId}`）



#### `{{CODE_TYPE_ID}}` — {{CODE_TYPE_DISPLAY_NAME}}



| 字段 | 内容 |

|------|------|

| codeTypeId | `{{CODE_TYPE_ID}}` |

| 显示名（中文） | {{CODE_TYPE_DISPLAY_NAME}} |

| 用途 | <!-- 中文 --> |

| 触发依据 | <!-- 中文；说明依据来自样例/文档/注册点等 --> |

| 样例文件 | <!-- 仓库相对路径列表，保持原文 --> |

| evidenceStrength | strong / medium / weak |

| status | draft / inferred（仅推断时必须为 inferred） |

| needsHumanConfirmation | true / false |

| 固定结构 | <!-- 中文描述；可含短摘录指引行号，避免大段粘贴源码 --> |

| 可变部分 | <!-- 中文 --> |

| 命名规律 | <!-- 中文；标识符举例保持原文 --> |

| 依赖关系 | <!-- 中文；模块/包/类型名保持原文 --> |

| 注册点 / 声明点 / 导出点 | <!-- 路径 + 标识符，保持原文；未知写「待确认」 --> |

| 是否需要多文件联动 | 是 / 否；说明 |

| 是否适合抽象为 Pattern Pack | <!-- 是/否/条件；仅为评估 --> |

| 是否需要人工确认 | 与 needsHumanConfirmation 一致；说明原因 |



## 待确认项



1. 



## 备注（预算与覆盖）



<!-- 若本轮深读截断、文件数未达上限、或某路径不可读，在此说明 -->


